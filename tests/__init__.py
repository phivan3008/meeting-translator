"""Test suite for the CPU-safe portions of the project.

Tests here must run on a Linux/Windows CPU environment without GPU, Qt, CUDA or
model weights. GPU and Windows-audio tests are marked separately.
"""
